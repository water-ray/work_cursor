---
description: 新项目目录布局与默认执行约束
alwaysApply: true
---

# 项目框架规则

## 目录布局

- 通用辅助目录统一使用 `scripts/`、`docs/`、`Bin/`、`temp/`；业务源码、配置和测试目录按项目技术栈的惯例组织，但不要把脚本、文档、产物或临时文件混放进去。
- 项目脚本默认使用 Python 编写，并统一存放到 `scripts/<项目类型或用途>/`，不要把脚本散落在仓库根目录或源码目录。
- 项目说明、设计文档、使用文档统一存放到 `docs/<项目或主题>/`；仓库总览可保留在根目录 `README.md`，详细内容放入 `docs/`。
- 编译、构建和打包后的最终成品统一输出到 `Bin/<项目>/`。
- 测试运行、脚本执行和处理中产生的临时文件统一写入 `temp/`，不要散落到源码目录、文档目录或仓库根目录。

## 结构约定

- 新增脚本或文档时，优先复用现有的 `scripts/<...>/` 或 `docs/<...>/` 子目录；仅在确有新类别时再新增一级子目录。
- 不因顺手整理而大范围搬迁现有文件；涉及目录重构时，仅处理用户明确要求的范围。
- 脚本输出、中间产物和测试产物不得提交到源码目录；应根据用途进入 `temp/` 或 `Bin/`。

## 执行边界

- 代码编写完成后，除非用户明确要求，不自动运行项目脚本、不启动服务、不执行编译或打包。
- 需要验证改动时，优先进行语法检查、lint、类型检查等静态校验。
- 如必须运行验证，仅运行最小必要的单元测试，避免额外启动完整流程或生成无关产物。
