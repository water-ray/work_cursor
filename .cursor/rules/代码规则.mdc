---
description: 通用代码协作与质量规则
alwaysApply: true
---

# 代码规则

## 上下文与沟通

- 先验证信息，只基于已确认的上下文回答；不要在没有明确证据时做出推测，也不要重复询问上下文中已提供的信息。
- 仅执行用户明确要求的修改；不要擅自扩展需求，也不要建议没有实际价值的改动（如纯空白调整、无必要改写）。
- 引用内容时使用真实文件路径；除非用户明确要求或确有必要，不主动展开当前实现细节。
- 同一文件尽量一次性给出完整修改，避免把同一个文件拆成多轮零碎指令。

## 修改边界

- 优先逐个文件处理，便于审阅和发现问题。
- 不删除无关代码、功能或结构；保持现有行为稳定，除非用户明确要求调整。
- 如果问题不需要实际改文件，就不要建议修改文件。

## 代码质量

- 遵循项目现有风格，优先使用描述清晰的命名，避免含糊缩写。
- 修改代码时同时考虑性能、安全、版本兼容性、边界情况和失败路径。
- 需要时补充合理的错误处理、日志、断言和命名常量，避免硬编码。
- 优先保持模块化和可维护性；新增或修改逻辑时，补充或建议最小必要的单元测试。
