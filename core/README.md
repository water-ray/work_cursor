# core

Go runtime core for routing, tunnel, and proxy orchestration.

## Responsibilities

- Manage engine lifecycle in an isolated runtime.
- Keep TUN adapter persistent across node switches.
- Expose stable C ABI for Flutter FFI integration.
